apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongodb.svc.headless" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "mongodb.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  publishNotReadyAddresses: true
  ports:
    - name: mongodb
      port: {{ .Values.mongodb.service.ports.mongodb.port }}
      targetPort: {{ .Values.mongodb.service.ports.mongodb.targetPort }}
      protocol: {{ .Values.mongodb.service.ports.mongodb.protocol }}
  selector:
    {{- include "mongodb.selectorLabels" . | nindent 4 }}